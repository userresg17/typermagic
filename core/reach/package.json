{
  "name": "@typer/reach",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Olhos na internet — ler/buscar web, YouTube, GitHub, RSS, Twitter/X, Reddit e mais, com roteamento por canal + cadeia de fallback + doctor (nativo, zero-dep; backends opcionais via cookie/MCP).",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  }
}
