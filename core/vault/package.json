{
  "name": "@typer/vault",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Vault cifrado (AES-256-GCM, node:crypto, zero dep) p/ credenciais e perfil do usuário — chave-mestra no keychain do SO (keytar, opcional) ou arquivo 0600. Segredos nunca em texto puro no disco.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -b"
  }
}
